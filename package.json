{
	"name": "@vafast/bearer",
	"version": "0.0.1",
	"description": "Middleware for Vafast for retrieving Bearer token",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"clean": "rimraf dist",
		"build": "tsup",
		"dev": "bun run --watch example/index.ts",
		"test": "bun test",
		"test:types": "tsc --noEmit",
		"lint": "eslint . --ext .ts",
		"lint:fix": "eslint . --ext .ts --fix",
		"type-check": "tsc --noEmit",
		"prepublishOnly": "npm run build && npm run test",
		"release": "npm run build && npm run test && npm publish --access=public",
		"release:patch": "npm version patch && npm run release && git push && git push --tags",
		"release:minor": "npm version minor && npm run release && git push && git push --tags",
		"release:major": "npm version major && npm run release && git push && git push --tags"
	},
	"peerDependencies": {
		"vafast": ">= 0.1.0"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"devDependencies": {
		"@types/bun": "^1.1.2",
		"@typescript-eslint/eslint-plugin": "^7.9.0",
		"@typescript-eslint/parser": "^7.9.0",
		"eslint": "^9.2.0",
		"rimraf": "^6.0.1",
		"tsup": "^8.5.1",
		"typescript": "^5.4.5",
		"vafast": "^0.3.4"
	},
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"keywords": [
		"vafast",
		"bearer",
		"authentication",
		"middleware"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/vafastjs/vafast-bearer"
	},
	"author": {
		"name": "saltyAom",
		"url": "https://github.com/SaltyAom",
		"email": "saltyaom@gmail.com"
	},
	"homepage": "https://github.com/vafastjs/vafast-bearer",
	"bugs": "https://github.com/vafastjs/vafast-bearer/issues"
}
